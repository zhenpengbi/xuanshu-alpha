# GUARDRAILS.md — 玄枢Alpha 建设纪律

> **本文件是项目约束文件，小老大（架构师）和 Claude Code（开发执行者）每次接新任务前必须检查。**
> 任何新需求必须通过本文件的「准入检查」才能排期开发。

---

## 一、核心目标（不可遗忘）

**玄枢的唯一目标：辅助毕老师做基金交易决策，带来可量化的理财收益。**

不是做好看的看板。不是展示技术能力。不是攒功能数量。
是让毕老师每次打开玄枢，能得到一个**可直接执行的操作建议**，并且这个建议**比不看玄枢要好**。

---

## 二、曾犯过的错误（引以为鉴）

### 2.1 目标漂移（2026-04~06）

| 错误 | 具体表现 | 正确做法 |
|---|---|---|
| 展示优先于决策 | 花大量时间在 Logo/UI/PWA/深色风格上 | 先跑通决策逻辑，再做呈现 |
| 技术信号选型过于基础 | RSI+MA+MACD 三指标对基金择时无效（回测已证明） | 回测结论出来后应立即重构信号体系 |
| 跳过估值模块 | 做了5个月，从未建"贵不贵"的判断能力 | 估值分位应是第一个建的模块 |
| 忽视风险度量 | 组合63%大宗暴露但从未预警 | 相关性/集中度应在持仓录入后立即建 |
| 回测结论未落地 | 发现趋势型择时无效，但没有据此修改建议逻辑 | 每个分析结论必须对应一个产品动作 |

### 2.2 根因总结

1. **架构师（小老大）出任务书时过度关注「可见性」（UI/部署/推送），忽视「有效性」（决策质量）**
2. **没有定期回检：做完一个模块后从未问"这个模块对操作建议的影响是什么"**
3. **把"有数据展示"等同于"有能力"——9只基金全部显示"观望"等于什么都没说**

---

## 三、准入检查（Gate Check）

### 每个新需求必须回答以下3个问题，缺一不可：

| # | 问题 | 不合格的回答 |
|---|---|---|
| 1 | **这个功能会改变操作建议吗？** | "不会，但展示更好看了" |
| 2 | **没有这个功能，毕老师会做出错误决策吗？** | "不会，只是信息不全" |
| 3 | **这个功能的优先级高于当前未完成的P0吗？** | "不确定" / 没有对比论证 |

**如果三个问题有任何一个答案是否定的，该需求应排到P0完成之后。**

### 当前 P0 清单（未完成不接新展示类需求）：

- [ ] **估值分位模块**：行业/指数PE/PB近5年百分位，输出"低估/适中/高估"
- [ ] **组合风险仪表盘**：波动率、相关性矩阵、大类集中度预警
- [ ] **信号体系重构**：根据回测结论，趋势型资产改为"定投/持有"建议，震荡型保留择时信号

---

## 四、架构师（小老大）的自检义务

### 4.1 出任务书前必须回答

1. 这个任务完成后，玄枢的**操作建议**会发生什么变化？
2. 如果答案是"不变"，为什么现在要做这个而不是做 P0？

### 4.2 每月回检（随记忆提炼 cron 一起）

1. 过去一个月新增的功能，哪些**实际改变了**操作建议的质量？
2. 有没有回测/分析结论产出后，**没有对应产品动作**的情况？
3. P0 清单的完成进度如何？是否被插入的需求挤占了？

### 4.3 禁止事项

- ❌ 不得在 P0 未完成时出 UI/美化/Logo/动画类任务书
- ❌ 不得把"展示更多数据"等同于"决策能力提升"
- ❌ 不得在回测得出结论后不修改对应的建议逻辑

---

## 五、开发执行者（Claude Code）的监督义务

### 5.1 接到任务书时

Claude Code 收到小老大的任务书后，**必须先对照本文件检查**：

1. 该任务是否通过了「三问准入检查」？
2. P0 清单是否已全部完成？如果没有，该任务是否属于 P0？
3. 如果任务不符合准入条件，**应当拒绝执行并回复小老大说明原因**

### 5.2 有权拒绝的场景

- 任务纯粹是 UI 美化，且 P0 未完成
- 任务增加了展示数据但不改变操作建议
- 任务与回测结论矛盾（如给趋势型资产增加更多择时信号）

### 5.3 有义务提醒的场景

- 发现代码中生成的建议全是同一个结论（如全部"观望"）
- 发现新增模块没有被接入到最终的操作建议输出中
- 发现估值/风险相关代码缺失但在做其他模块

---

## 六、决策质量评估标准

玄枢的操作建议好不好，用以下标准衡量：

| 维度 | 合格线 | 不合格 |
|---|---|---|
| 区分度 | 9只基金中至少有2-3只给出不同建议 | 全部"观望"或全部"持有" |
| 可操作性 | 给出具体金额/比例/时间节奏 | 只说"可小幅加仓" |
| 有依据 | 每个建议附带数据支撑（估值分位/偏离度/信号） | 只有技术指标罗列 |
| 事后可验证 | 建议发出30天后可对比实际收益 | 建议模糊到无法验证 |

---

## 七、生效日期与修订

- 生效日期：2026-06-17
- 制定人：小老大（架构师）
- 适用范围：玄枢Alpha 项目所有后续开发
- 修订权限：毕老师可随时修改；小老大和 Claude Code 均不可单方面删除本文件中的约束条款

---

*本文件的存在本身就是一个教训的产物。希望不再需要第二份。*
